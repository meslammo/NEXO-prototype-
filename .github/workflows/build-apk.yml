name: Build NEXO APK

on:
  workflow_dispatch:
  push:
    branches: ["nexo/fullstack-phases-1-10"]

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.24.5"
          channel: stable
          cache: true
      - name: Flutter pub get
        run: flutter pub get
      - name: Analyze (non-blocking)
        run: flutter analyze || true
      - name: Regenerate supported Android host project
        run: flutter create --platforms=android --project-name nexo_app --org com.example .
      - name: Patch network and call permissions
        run: |
          python3 -c 'from pathlib import Path; p=Path("android/app/src/main/AndroidManifest.xml"); s=p.read_text(); perms="  <uses-permission android:name=\"android.permission.INTERNET\"/>\\n  <uses-permission android:name=\"android.permission.RECORD_AUDIO\"/>\\n  <uses-permission android:name=\"android.permission.CAMERA\"/>\\n"; p.write_text(s.replace("</manifest>", perms+"</manifest>"))'
      - name: Build APK
        run: flutter build apk --release --dart-define=NEXO_API_URL=https://nexo-api-production-05d3.up.railway.app
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: nexo-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
